# Plivo CLI — command reference

_Generated by `make docs` from the CLI's own command tree. Do not edit by hand._

## Commands

- [`plivo`](#plivo) — Plivo CLI — manage messaging, voice, numbers, applications
- [`plivo account`](#plivo-account) — Inspect and update the active Plivo account
- [`plivo account applications`](#plivo-account-applications) — Manage Plivo applications (voice/messaging webhooks)
- [`plivo account applications create`](#plivo-account-applications-create) — Create a new application
- [`plivo account applications delete`](#plivo-account-applications-delete) — Delete an application (requires --yes)
- [`plivo account applications get`](#plivo-account-applications-get) — Get application details
- [`plivo account applications list`](#plivo-account-applications-list) — List applications
- [`plivo account applications update`](#plivo-account-applications-update) — Update an application
- [`plivo account get`](#plivo-account-get) — Get account details (name, billing mode, credits, address)
- [`plivo account subaccounts`](#plivo-account-subaccounts) — Manage subaccounts under the master account
- [`plivo account subaccounts create`](#plivo-account-subaccounts-create) — Create a subaccount
- [`plivo account subaccounts delete`](#plivo-account-subaccounts-delete) — Delete a subaccount (requires --yes)
- [`plivo account subaccounts get`](#plivo-account-subaccounts-get) — Get a subaccount by auth_id
- [`plivo account subaccounts list`](#plivo-account-subaccounts-list) — List subaccounts
- [`plivo account subaccounts update`](#plivo-account-subaccounts-update) — Update a subaccount
- [`plivo account update`](#plivo-account-update) — Update account profile fields
- [`plivo agent`](#plivo-agent) — Manage Plivo Vibe AI agents (coming soon)
- [`plivo ask`](#plivo-ask) — Ask Plivo's AI assistant (streams the answer as it comes)
- [`plivo auth`](#plivo-auth) — Manage credential profiles (list / use / remove / whoami)
- [`plivo auth list`](#plivo-auth-list) — List configured profiles
- [`plivo auth remove`](#plivo-auth-remove) — Remove a profile (use `plivo logout` for the active profile)
- [`plivo auth use`](#plivo-auth-use) — Set the active profile
- [`plivo auth whoami`](#plivo-auth-whoami) — Verify credentials and print active account
- [`plivo login`](#plivo-login) — Log in to Plivo (saves credentials securely to the OS keychain)
- [`plivo logout`](#plivo-logout) — Log out a profile (deletes its entry + removes the auth_token from the OS keychain)
- [`plivo lookup`](#plivo-lookup) — Carrier + format lookup for an E.164 number (lookup.plivo.com)
- [`plivo message`](#plivo-message) — Send messages (SMS/MMS/WhatsApp); manage 10DLC, powerpacks, toll-free
- [`plivo message 10dlc`](#plivo-message-10dlc) — US A2P 10DLC registration — brands, campaigns, links
- [`plivo message 10dlc brands`](#plivo-message-10dlc-brands) — 10DLC brand registration (US A2P 10-digit-long-code messaging)
- [`plivo message 10dlc brands create`](#plivo-message-10dlc-brands-create) — Register a new brand (spends money — TCR registration fee, requires --yes)
- [`plivo message 10dlc brands get`](#plivo-message-10dlc-brands-get) — Get a brand by ID
- [`plivo message 10dlc brands list`](#plivo-message-10dlc-brands-list) — List registered brands
- [`plivo message 10dlc brands update`](#plivo-message-10dlc-brands-update) — Update mutable brand fields
- [`plivo message 10dlc campaigns`](#plivo-message-10dlc-campaigns) — 10DLC campaign registration (use cases for a brand)
- [`plivo message 10dlc campaigns create`](#plivo-message-10dlc-campaigns-create) — Register a new campaign (spends money — TCR fee, requires --yes)
- [`plivo message 10dlc campaigns get`](#plivo-message-10dlc-campaigns-get) — Get a campaign by ID
- [`plivo message 10dlc campaigns list`](#plivo-message-10dlc-campaigns-list) — List campaigns
- [`plivo message 10dlc campaigns update`](#plivo-message-10dlc-campaigns-update) — Update mutable campaign fields
- [`plivo message 10dlc links`](#plivo-message-10dlc-links) — 10DLC number-to-campaign linking
- [`plivo message 10dlc links create`](#plivo-message-10dlc-links-create) — Link a number to a campaign
- [`plivo message 10dlc links delete`](#plivo-message-10dlc-links-delete) — Unlink a number (requires --yes)
- [`plivo message 10dlc links list`](#plivo-message-10dlc-links-list) — List number→campaign links
- [`plivo message get`](#plivo-message-get) — Get a message by UUID
- [`plivo message list`](#plivo-message-list) — List messages
- [`plivo message powerpacks`](#plivo-message-powerpacks) — Powerpacks — number pools for high-volume SMS
- [`plivo message powerpacks create`](#plivo-message-powerpacks-create) — Create a powerpack
- [`plivo message powerpacks delete`](#plivo-message-powerpacks-delete) — Delete a powerpack (requires --yes)
- [`plivo message powerpacks get`](#plivo-message-powerpacks-get) — Get a powerpack by uuid
- [`plivo message powerpacks list`](#plivo-message-powerpacks-list) — List powerpacks
- [`plivo message powerpacks numbers`](#plivo-message-powerpacks-numbers) — Manage numbers inside a powerpack
- [`plivo message powerpacks numbers add`](#plivo-message-powerpacks-numbers-add) — Add a number to a powerpack
- [`plivo message powerpacks numbers list`](#plivo-message-powerpacks-numbers-list) — List numbers attached to a powerpack
- [`plivo message powerpacks numbers remove`](#plivo-message-powerpacks-numbers-remove) — Remove a number from a powerpack (requires --yes)
- [`plivo message powerpacks update`](#plivo-message-powerpacks-update) — Update a powerpack
- [`plivo message send`](#plivo-message-send) — Send an SMS/MMS (defaults to dry-run; pass --yes to actually send)
- [`plivo message tollfree`](#plivo-message-tollfree) — Toll-free verification (US TFN messaging compliance)
- [`plivo message tollfree get`](#plivo-message-tollfree-get) — Get a toll-free verification profile by uuid
- [`plivo message tollfree list`](#plivo-message-tollfree-list) — List toll-free verification profiles
- [`plivo message tollfree submit`](#plivo-message-tollfree-submit) — Submit a new toll-free verification profile
- [`plivo numbers`](#plivo-numbers) — Manage account phone numbers
- [`plivo numbers buy`](#plivo-numbers-buy) — Rent a phone number (requires --yes; spends money)
- [`plivo numbers cnam`](#plivo-numbers-cnam) — Caller-ID Name (CNAM) lookup for a US/CA number (spends money — requires --yes)
- [`plivo numbers compliance`](#plivo-numbers-compliance) — Regulatory compliance for phone numbers (requirements, applications, linking)
- [`plivo numbers compliance create`](#plivo-numbers-compliance-create) — Create + submit a compliance application (multipart; auto-submits)
- [`plivo numbers compliance delete`](#plivo-numbers-compliance-delete) — Delete a compliance application (requires --yes)
- [`plivo numbers compliance get`](#plivo-numbers-compliance-get) — Get a compliance application by ID
- [`plivo numbers compliance link`](#plivo-numbers-compliance-link) — Bulk-link numbers to accepted compliance applications
- [`plivo numbers compliance list`](#plivo-numbers-compliance-list) — List compliance applications
- [`plivo numbers compliance requirements`](#plivo-numbers-compliance-requirements) — List documents/fields required to activate a regulated number
- [`plivo numbers compliance update`](#plivo-numbers-compliance-update) — Update a rejected compliance application (multipart; auto-resubmits)
- [`plivo numbers get`](#plivo-numbers-get) — Get details of a rented number
- [`plivo numbers list`](#plivo-numbers-list) — List numbers rented to your account
- [`plivo numbers masking`](#plivo-numbers-masking) — Number-Masking sessions (privacy-preserving call/SMS bridge)
- [`plivo numbers masking sessions`](#plivo-numbers-masking-sessions) — Manage number-masking sessions
- [`plivo numbers masking sessions create`](#plivo-numbers-masking-sessions-create) — Create a masking session (spends money — requires --yes)
- [`plivo numbers masking sessions delete`](#plivo-numbers-masking-sessions-delete) — End a masking session (requires --yes)
- [`plivo numbers masking sessions get`](#plivo-numbers-masking-sessions-get) — Get a masking session by uuid
- [`plivo numbers masking sessions list`](#plivo-numbers-masking-sessions-list) — List masking sessions
- [`plivo numbers release`](#plivo-numbers-release) — Release a rented number (requires --yes; stops monthly billing)
- [`plivo numbers search`](#plivo-numbers-search) — Search available numbers to rent
- [`plivo numbers update`](#plivo-numbers-update) — Update settings on a rented number
- [`plivo support`](#plivo-support) — List your past support escalations (filed via `plivo ask`)
- [`plivo upgrade`](#plivo-upgrade) — Upgrade plivo to the latest release
- [`plivo verify`](#plivo-verify) — Plivo Verify — OTP / phone-number verification sessions
- [`plivo verify sessions`](#plivo-verify-sessions) — Manage Verify sessions
- [`plivo verify sessions create`](#plivo-verify-sessions-create) — Create a Verify session (spends money — requires --yes)
- [`plivo verify sessions get`](#plivo-verify-sessions-get) — Get a Verify session by uuid
- [`plivo verify sessions list`](#plivo-verify-sessions-list) — List Verify sessions
- [`plivo verify sessions validate`](#plivo-verify-sessions-validate) — Submit the OTP to validate a session
- [`plivo voice`](#plivo-voice) — Voice — calls, conferences, multiparty, recordings, endpoints
- [`plivo voice calls`](#plivo-voice-calls) — Make and inspect voice calls
- [`plivo voice calls dtmf`](#plivo-voice-calls-dtmf) — Send DTMF tones into a live call
- [`plivo voice calls get`](#plivo-voice-calls-get) — Get a call by UUID
- [`plivo voice calls hangup`](#plivo-voice-calls-hangup) — Hang up a live call (requires --yes)
- [`plivo voice calls list`](#plivo-voice-calls-list) — List calls
- [`plivo voice calls make`](#plivo-voice-calls-make) — Make an outbound call (defaults to dry-run; pass --yes to actually dial)
- [`plivo voice calls play`](#plivo-voice-calls-play) — Play audio file(s) into a live call
- [`plivo voice calls record`](#plivo-voice-calls-record) — Start recording a live call
- [`plivo voice calls speak`](#plivo-voice-calls-speak) — Speak text into a live call via TTS
- [`plivo voice calls stop-play`](#plivo-voice-calls-stop-play) — Stop any audio currently playing in a live call
- [`plivo voice calls stop-record`](#plivo-voice-calls-stop-record) — Stop the active recording on a live call
- [`plivo voice calls stop-speak`](#plivo-voice-calls-stop-speak) — Stop any TTS currently playing in a live call
- [`plivo voice calls streams`](#plivo-voice-calls-streams) — Live audio streams on a call (WebSocket bridge for transcription / agents)
- [`plivo voice calls streams get`](#plivo-voice-calls-streams-get) — Get details for one stream on a call
- [`plivo voice calls streams list`](#plivo-voice-calls-streams-list) — List active streams on a call
- [`plivo voice calls streams start`](#plivo-voice-calls-streams-start) — Start a new audio stream on a call
- [`plivo voice calls streams stop`](#plivo-voice-calls-streams-stop) — Stop one stream (with id) or all streams (without id) on a call
- [`plivo voice calls transfer`](#plivo-voice-calls-transfer) — Transfer one or both legs of a live call to new PlivoXML URLs
- [`plivo voice conferences`](#plivo-voice-conferences) — Inspect and control live audio conferences
- [`plivo voice conferences get`](#plivo-voice-conferences-get) — Get conference details (members, run time)
- [`plivo voice conferences hangup`](#plivo-voice-conferences-hangup) — End the entire conference (requires --yes)
- [`plivo voice conferences list`](#plivo-voice-conferences-list) — List active conference names
- [`plivo voice conferences member`](#plivo-voice-conferences-member) — Per-member actions inside a conference
- [`plivo voice conferences member deaf`](#plivo-voice-conferences-member-deaf) — Deafen a member (they can't hear)
- [`plivo voice conferences member kick`](#plivo-voice-conferences-member-kick) — Kick a member from the conference (requires --yes)
- [`plivo voice conferences member mute`](#plivo-voice-conferences-member-mute) — Mute a member
- [`plivo voice conferences member play`](#plivo-voice-conferences-member-play) — Play audio file(s) into a member's channel
- [`plivo voice conferences member speak`](#plivo-voice-conferences-member-speak) — Speak TTS text to a member
- [`plivo voice conferences member stop-play`](#plivo-voice-conferences-member-stop-play) — Stop audio playback to a member
- [`plivo voice conferences member stop-speak`](#plivo-voice-conferences-member-stop-speak) — Stop TTS playback to a member
- [`plivo voice conferences member undeaf`](#plivo-voice-conferences-member-undeaf) — Undeafen a member
- [`plivo voice conferences member unmute`](#plivo-voice-conferences-member-unmute) — Unmute a member
- [`plivo voice conferences record`](#plivo-voice-conferences-record) — Start recording a conference
- [`plivo voice conferences stop-record`](#plivo-voice-conferences-stop-record) — Stop the active recording on a conference
- [`plivo voice endpoints`](#plivo-voice-endpoints) — Manage SIP endpoints (registered SIP devices/usernames)
- [`plivo voice endpoints create`](#plivo-voice-endpoints-create) — Create a SIP endpoint
- [`plivo voice endpoints delete`](#plivo-voice-endpoints-delete) — Delete a SIP endpoint (requires --yes)
- [`plivo voice endpoints get`](#plivo-voice-endpoints-get) — Get an endpoint by ID
- [`plivo voice endpoints list`](#plivo-voice-endpoints-list) — List SIP endpoints
- [`plivo voice endpoints update`](#plivo-voice-endpoints-update) — Update a SIP endpoint
- [`plivo voice multiparty`](#plivo-voice-multiparty) — Multi-Party Calls (MPC) — group voice rooms with dynamic participants
- [`plivo voice multiparty create`](#plivo-voice-multiparty-create) — Create a multi-party call (spends money — requires --yes)
- [`plivo voice multiparty end`](#plivo-voice-multiparty-end) — End the MPC and hang up all participants (requires --yes)
- [`plivo voice multiparty get`](#plivo-voice-multiparty-get) — Get an MPC by uuid or friendly_name
- [`plivo voice multiparty list`](#plivo-voice-multiparty-list) — List multi-party calls
- [`plivo voice multiparty participant`](#plivo-voice-multiparty-participant) — Per-participant actions inside an MPC
- [`plivo voice multiparty participant add`](#plivo-voice-multiparty-participant-add) — Add a participant by dialing out (spends money — requires --yes)
- [`plivo voice multiparty participant hold`](#plivo-voice-multiparty-participant-hold) — Put a participant on hold
- [`plivo voice multiparty participant kick`](#plivo-voice-multiparty-participant-kick) — Remove a participant (requires --yes)
- [`plivo voice multiparty participant list`](#plivo-voice-multiparty-participant-list) — List participants in an MPC
- [`plivo voice multiparty participant mute`](#plivo-voice-multiparty-participant-mute) — Mute a participant
- [`plivo voice multiparty participant unhold`](#plivo-voice-multiparty-participant-unhold) — Take a participant off hold
- [`plivo voice multiparty participant unmute`](#plivo-voice-multiparty-participant-unmute) — Unmute a participant
- [`plivo voice recordings`](#plivo-voice-recordings) — List, fetch, and delete call/conference recordings
- [`plivo voice recordings delete`](#plivo-voice-recordings-delete) — Delete a recording (requires --yes)
- [`plivo voice recordings get`](#plivo-voice-recordings-get) — Get a recording by ID
- [`plivo voice recordings list`](#plivo-voice-recordings-list) — List recordings

---

## `plivo`

Plivo CLI — manage messaging, voice, numbers, applications

plivo is a command-line interface for the Plivo REST API.

Credentials resolve in order:
  1. --profile flag
  2. active profile in ~/.plivo/config.toml
  3. PLIVO_AUTH_ID / PLIVO_AUTH_TOKEN env vars

```
plivo
plivo [command]
```

Subcommands:

- `account` — Inspect and update the active Plivo account
- `agent` — Manage Plivo Vibe AI agents (coming soon)
- `ask` — Ask Plivo's AI assistant (streams the answer as it comes)
- `auth` — Manage credential profiles (list / use / remove / whoami)
- `login` — Log in to Plivo (saves credentials securely to the OS keychain)
- `logout` — Log out a profile (deletes its entry + removes the auth_token from the OS keychain)
- `lookup` — Carrier + format lookup for an E.164 number (lookup.plivo.com)
- `message` — Send messages (SMS/MMS/WhatsApp); manage 10DLC, powerpacks, toll-free
- `numbers` — Manage account phone numbers
- `support` — List your past support escalations (filed via `plivo ask`)
- `upgrade` — Upgrade plivo to the latest release
- `verify` — Plivo Verify — OTP / phone-number verification sessions
- `voice` — Voice — calls, conferences, multiparty, recordings, endpoints

Flags:

```
      --all                auto-paginate through all pages
      --dry-run            print the HTTP request without sending
      --explain            explain what the command will do before executing
      --log-level string   log level: debug|info|warn|error|none (default "warn")
      --no-color           disable colored output
  -o, --output string      output format: table|json (default: table for TTY, json otherwise)
      --profile string     named profile from ~/.plivo/config.toml
  -q, --quiet              suppress non-data output
      --timeout int        request timeout in seconds (default 30)
  -y, --yes                skip confirmation prompts
```

---

## `plivo account`

Inspect and update the active Plivo account

```
plivo account
plivo account [command]
```

Subcommands:

- `applications` — Manage Plivo applications (voice/messaging webhooks)
- `get` — Get account details (name, billing mode, credits, address)
- `subaccounts` — Manage subaccounts under the master account
- `update` — Update account profile fields

---

## `plivo account applications`

Manage Plivo applications (voice/messaging webhooks)

```
plivo account applications
plivo account applications [command]
```

Aliases: `app`, `application`

Subcommands:

- `create` — Create a new application
- `delete` — Delete an application (requires --yes)
- `get` — Get application details
- `list` — List applications
- `update` — Update an application

---

## `plivo account applications create`

Create a new application

```
plivo account applications create [flags]
```

Flags:

```
      --answer-method string         GET|POST (default "POST")
      --answer-url string            webhook for incoming calls (required)
      --app-name string              application name (required)
      --default-number-app           set as default for new numbers
      --fallback-answer-url string   backup webhook if answer-url fails
      --hangup-url string            webhook for call hangup
      --log-incoming-messages        log inbound SMS content (default true)
      --message-url string           webhook for inbound SMS
```

---

## `plivo account applications delete`

Delete an application (requires --yes)

```
plivo account applications delete <app_id> [flags]
```

Flags:

```
      --cascade   also detach numbers/endpoints
```

---

## `plivo account applications get`

Get application details

```
plivo account applications get <app_id>
```

---

## `plivo account applications list`

List applications

```
plivo account applications list [flags]
```

Flags:

```
      --limit int    results per page (default 20)
      --offset int   pagination offset
```

---

## `plivo account applications update`

Update an application

```
plivo account applications update <app_id> [flags]
```

Flags:

```
      --answer-method string   GET|POST
      --answer-url string      new answer URL
      --app-name string        new application name
      --hangup-url string      new hangup URL
      --message-url string     new message URL
```

---

## `plivo account get`

Get account details (name, billing mode, credits, address)

```
plivo account get
```

---

## `plivo account subaccounts`

Manage subaccounts under the master account

```
plivo account subaccounts
plivo account subaccounts [command]
```

Aliases: `sub`, `subaccount`

Subcommands:

- `create` — Create a subaccount
- `delete` — Delete a subaccount (requires --yes)
- `get` — Get a subaccount by auth_id
- `list` — List subaccounts
- `update` — Update a subaccount

---

## `plivo account subaccounts create`

Create a subaccount

```
plivo account subaccounts create [flags]
```

Flags:

```
      --enabled       enable on creation (default true)
      --name string   subaccount name (required)
```

---

## `plivo account subaccounts delete`

Delete a subaccount (requires --yes)

```
plivo account subaccounts delete <subauth_id>
```

---

## `plivo account subaccounts get`

Get a subaccount by auth_id

```
plivo account subaccounts get <subauth_id>
```

---

## `plivo account subaccounts list`

List subaccounts

```
plivo account subaccounts list [flags]
```

Flags:

```
      --limit int    results per page (default 20)
      --offset int   pagination offset
```

---

## `plivo account subaccounts update`

Update a subaccount

```
plivo account subaccounts update <subauth_id> [flags]
```

Flags:

```
      --enabled string   true|false
      --name string      new name
```

---

## `plivo account update`

Update account profile fields

```
plivo account update [flags]
```

Flags:

```
      --address string    billing address
      --city string       city
      --name string       account name
      --timezone string   IANA timezone (e.g. Asia/Kolkata)
```

---

## `plivo agent`

Manage Plivo Vibe AI agents (coming soon)

Plivo Vibe AI agents — build, publish, and run AI voice agents from your terminal.

Coming soon — this surface is not part of the current release.
Track progress: https://github.com/plivo/plivo-cli/blob/main/ROADMAP.md

```
plivo agent
```

---

## `plivo ask`

Ask Plivo's AI assistant (streams the answer as it comes)

Send a single message to Plivo's AI assistant and stream the response.

The assistant uses Server-Sent Events: token text streams to stdout as it
arrives, live status appears on stderr and is overwritten in place, and the
final summary lands on stdout. Long flows (voice-debug can run 2–5 minutes)
work fine — there is no overall HTTP timeout. Ctrl-C cancels and exits 130;
no auto-retry (debugger runs are not idempotent).

Pass --call-uuid for voice-debug context; --verbose to show the assistant's
tool calls; -o json to emit each SSE event as one JSONL line (handy for
scripts and AI agents).

```
plivo ask <message> [flags]
```

Examples:

```
  plivo ask "What does Plivo SMS error code 30007 mean?"
  plivo ask --call-uuid 21e68d29-... "Debug what happened on this call"
  plivo ask -o json "What's the rate for outbound voice to Brazil?"
```

Flags:

```
      --buddy-url string   override the assistant's hodor URL (also via PLIVO_BUDDY_URL env, or [buddy].hodor_url in config)
      --call-uuid string   voice-debug context: the call UUID the assistant should analyse
      --debug-stream       log every raw SSE frame to stderr (for debugging this CLI)
      --verbose            show the assistant's tool_call / tool_output events on stderr
```

---

## `plivo auth`

Manage credential profiles (list / use / remove / whoami)

```
plivo auth
plivo auth [command]
```

Subcommands:

- `list` — List configured profiles
- `remove` — Remove a profile (use `plivo logout` for the active profile)
- `use` — Set the active profile
- `whoami` — Verify credentials and print active account

---

## `plivo auth list`

List configured profiles

```
plivo auth list
```

---

## `plivo auth remove`

Remove a profile (use `plivo logout` for the active profile)

```
plivo auth remove <profile>
```

---

## `plivo auth use`

Set the active profile

```
plivo auth use <profile>
```

---

## `plivo auth whoami`

Verify credentials and print active account

```
plivo auth whoami
```

---

## `plivo login`

Log in to Plivo (saves credentials securely to the OS keychain)

Log in to Plivo and save credentials for subsequent commands.

The auth_token is stored in your OS keychain (macOS Keychain, Windows
Credential Manager, Linux Secret Service). The CLI falls back to writing
the token inline in ~/.plivo/config.toml (chmod 0600) only when no
keychain is available.

By default, the CLI validates the credentials by calling GET /Account/
before saving — use --no-verify to skip if you're offline or hitting a
local mock.

```
plivo login [flags]
```

Examples:

```
  plivo login --browser                             # opens browser (recommended)
  plivo login                                       # interactive prompts
  plivo login --auth-id MAxxxxxxxxxxxxxxxxxxxx      # prompts for token only
  echo "$TOKEN" | plivo login --auth-id MAxx --auth-token-stdin
  plivo login --name staging                        # save under a named profile
```

Flags:

```
      --auth-id string     Plivo auth_id (prompted if omitted)
      --auth-token-stdin   read auth_token from stdin (for CI / scripts); hidden interactive prompt otherwise
      --browser            log in via your default browser (PKCE loopback OAuth); recommended
      --env string         target environment (default "prod"; e.g. "dev" — internal builds only). Persisted on the profile so subsequent commands inherit it.
  -n, --name string        profile name to save under (default "default")
      --no-verify          skip the GET /Account/ validation hit (offline / mock use only)
```

---

## `plivo logout`

Log out a profile (deletes its entry + removes the auth_token from the OS keychain)

Log out a profile. With no argument, logs out the active profile.

Removes the profile row from ~/.plivo/config.toml and best-effort deletes
the auth_token from the OS keychain. If the active profile was logged
out, no profile becomes active until you `plivo auth use <name>` (or
log in again).

```
plivo logout [profile]
```

Examples:

```
  plivo logout              # active profile
  plivo logout staging      # named profile
```

---

## `plivo lookup`

Carrier + format lookup for an E.164 number (lookup.plivo.com)

```
plivo lookup <number> [flags]
```

Flags:

```
      --type string   lookup type (currently only 'carrier' is supported by Plivo) (default "carrier")
```

---

## `plivo message`

Send messages (SMS/MMS/WhatsApp); manage 10DLC, powerpacks, toll-free

```
plivo message
plivo message [command]
```

Aliases: `msg`, `sms`

Subcommands:

- `10dlc` — US A2P 10DLC registration — brands, campaigns, links
- `get` — Get a message by UUID
- `list` — List messages
- `powerpacks` — Powerpacks — number pools for high-volume SMS
- `send` — Send an SMS/MMS (defaults to dry-run; pass --yes to actually send)
- `tollfree` — Toll-free verification (US TFN messaging compliance)

---

## `plivo message 10dlc`

US A2P 10DLC registration — brands, campaigns, links

```
plivo message 10dlc
plivo message 10dlc [command]
```

Subcommands:

- `brands` — 10DLC brand registration (US A2P 10-digit-long-code messaging)
- `campaigns` — 10DLC campaign registration (use cases for a brand)
- `links` — 10DLC number-to-campaign linking

---

## `plivo message 10dlc brands`

10DLC brand registration (US A2P 10-digit-long-code messaging)

```
plivo message 10dlc brands
plivo message 10dlc brands [command]
```

Aliases: `brand`

Subcommands:

- `create` — Register a new brand (spends money — TCR registration fee, requires --yes)
- `get` — Get a brand by ID
- `list` — List registered brands
- `update` — Update mutable brand fields

---

## `plivo message 10dlc brands create`

Register a new brand (spends money — TCR registration fee, requires --yes)

```
plivo message 10dlc brands create [flags]
```

Flags:

```
      --alias string                 human-friendly alias (required)
      --brand-type string            STANDARD|LOW_VOLUME_STANDARD|SOLE_PROPRIETOR (default "STANDARD")
      --ein string                   tax ID / EIN
      --ein-issuing-country string   ISO-2 country issuing the EIN (default "US")
      --email string                 support email
      --entity-type string           PRIVATE_PROFIT|PUBLIC_PROFIT|NON_PROFIT|GOVERNMENT|SOLE_PROPRIETOR (default "PRIVATE_PROFIT")
      --legal-name string            legal entity name (required)
      --phone string                 support phone E.164
      --stock-exchange string        exchange code (PUBLIC_PROFIT only)
      --stock-symbol string          ticker (PUBLIC_PROFIT only)
      --vertical string              industry vertical (e.g. TECHNOLOGY, RETAIL)
      --website string               primary website URL
```

---

## `plivo message 10dlc brands get`

Get a brand by ID

```
plivo message 10dlc brands get <brand_id>
```

---

## `plivo message 10dlc brands list`

List registered brands

```
plivo message 10dlc brands list [flags]
```

Flags:

```
      --limit int    results per page (default 20)
      --offset int   pagination offset
```

---

## `plivo message 10dlc brands update`

Update mutable brand fields

```
plivo message 10dlc brands update <brand_id> [flags]
```

Flags:

```
      --email string     support email
      --phone string     support phone
      --website string   website URL
```

---

## `plivo message 10dlc campaigns`

10DLC campaign registration (use cases for a brand)

```
plivo message 10dlc campaigns
plivo message 10dlc campaigns [command]
```

Aliases: `camp`, `campaign`

Subcommands:

- `create` — Register a new campaign (spends money — TCR fee, requires --yes)
- `get` — Get a campaign by ID
- `list` — List campaigns
- `update` — Update mutable campaign fields

---

## `plivo message 10dlc campaigns create`

Register a new campaign (spends money — TCR fee, requires --yes)

```
plivo message 10dlc campaigns create [flags]
```

Flags:

```
      --affiliate-marketing       affiliate marketing campaign
      --age-gated                 campaign targets adult content
      --alias string              human-friendly alias (required)
      --brand-id string           brand to register under (required)
      --description string        what this campaign does (required)
      --direct-lending            direct-lending arrangements
      --embedded-link             messages contain links
      --embedded-phone            messages contain phone numbers
      --help-keywords string      comma-separated help keywords (default "HELP")
      --help-message string       auto-reply to HELP
      --message-flow string       describe how recipients opt in (required)
      --number-pool               campaign uses a number pool
      --opt-in-keywords string    opt-in keywords (default "START")
      --opt-in-message string     opt-in confirmation message
      --opt-out-keywords string   opt-out keywords (default "STOP")
      --opt-out-message string    opt-out confirmation message
      --sample-message-1 string   sample message (required)
      --sample-message-2 string   second sample message
      --sub-usecases string       comma-separated sub use cases
      --usecase string            primary use case, e.g. MARKETING, MIXED (required)
```

---

## `plivo message 10dlc campaigns get`

Get a campaign by ID

```
plivo message 10dlc campaigns get <campaign_id>
```

---

## `plivo message 10dlc campaigns list`

List campaigns

```
plivo message 10dlc campaigns list [flags]
```

Flags:

```
      --brand-id string   filter by brand_id
      --limit int         results per page (default 20)
      --offset int        pagination offset
```

---

## `plivo message 10dlc campaigns update`

Update mutable campaign fields

```
plivo message 10dlc campaigns update <campaign_id> [flags]
```

Flags:

```
      --description string        new description
      --message-flow string       new message flow
      --sample-message-1 string   new sample 1
      --sample-message-2 string   new sample 2
```

---

## `plivo message 10dlc links`

10DLC number-to-campaign linking

```
plivo message 10dlc links
plivo message 10dlc links [command]
```

Aliases: `link`

Subcommands:

- `create` — Link a number to a campaign
- `delete` — Unlink a number (requires --yes)
- `list` — List number→campaign links

---

## `plivo message 10dlc links create`

Link a number to a campaign

```
plivo message 10dlc links create [flags]
```

Flags:

```
      --campaign-id string   10DLC campaign id (required)
      --number string        E.164 number (required)
```

---

## `plivo message 10dlc links delete`

Unlink a number (requires --yes)

```
plivo message 10dlc links delete <link_id>
```

---

## `plivo message 10dlc links list`

List number→campaign links

```
plivo message 10dlc links list [flags]
```

Flags:

```
      --campaign-id string   filter by campaign_id
      --limit int            results per page (default 20)
      --number string        filter by number
      --offset int           pagination offset
```

---

## `plivo message get`

Get a message by UUID

```
plivo message get <message_uuid>
```

---

## `plivo message list`

List messages

```
plivo message list [flags]
```

Flags:

```
      --direction string   inbound|outbound
      --from string        filter by from_number
      --limit int          results per page (default 20)
      --offset int         pagination offset
      --state string       queued|sent|delivered|undelivered|failed|received
      --to string          filter by to_number
      --type string        sms|mms|whatsapp
```

---

## `plivo message powerpacks`

Powerpacks — number pools for high-volume SMS

```
plivo message powerpacks
plivo message powerpacks [command]
```

Aliases: `pp`, `powerpack`

Subcommands:

- `create` — Create a powerpack
- `delete` — Delete a powerpack (requires --yes)
- `get` — Get a powerpack by uuid
- `list` — List powerpacks
- `numbers` — Manage numbers inside a powerpack
- `update` — Update a powerpack

---

## `plivo message powerpacks create`

Create a powerpack

```
plivo message powerpacks create [flags]
```

Flags:

```
      --application-id string     application uuid (when application-type=xml_application)
      --application-type string   default_message|xml_application
      --local-connect             use a local-prefix-matching number
      --name string               powerpack name (required)
      --number-priority string    ordered priority list (e.g. "local,tollfree")
      --sticky-sender             send from same number to same recipient
```

---

## `plivo message powerpacks delete`

Delete a powerpack (requires --yes)

```
plivo message powerpacks delete <uuid>
```

---

## `plivo message powerpacks get`

Get a powerpack by uuid

```
plivo message powerpacks get <uuid>
```

---

## `plivo message powerpacks list`

List powerpacks

```
plivo message powerpacks list [flags]
```

Flags:

```
      --limit int    results per page (default 20)
      --offset int   pagination offset
```

---

## `plivo message powerpacks numbers`

Manage numbers inside a powerpack

```
plivo message powerpacks numbers
plivo message powerpacks numbers [command]
```

Aliases: `number`

Subcommands:

- `add` — Add a number to a powerpack
- `list` — List numbers attached to a powerpack
- `remove` — Remove a number from a powerpack (requires --yes)

---

## `plivo message powerpacks numbers add`

Add a number to a powerpack

```
plivo message powerpacks numbers add <uuid> <number>
```

---

## `plivo message powerpacks numbers list`

List numbers attached to a powerpack

```
plivo message powerpacks numbers list <uuid> [flags]
```

Flags:

```
      --limit int    results per page (default 20)
      --offset int   pagination offset
```

---

## `plivo message powerpacks numbers remove`

Remove a number from a powerpack (requires --yes)

```
plivo message powerpacks numbers remove <uuid> <number>
```

---

## `plivo message powerpacks update`

Update a powerpack

```
plivo message powerpacks update <uuid> [flags]
```

Flags:

```
      --local-connect string   true|false
      --name string            new name
      --sticky-sender string   true|false
```

---

## `plivo message send`

Send an SMS/MMS (defaults to dry-run; pass --yes to actually send)

```
plivo message send [flags]
```

Flags:

```
      --dst <           destination, separate multiple with < (required)
      --method string   callback method GET|POST (default "POST")
      --src string      sender (E.164, shortcode, or sender ID) (required)
      --text string     message body (required)
      --type string     sms|mms|whatsapp (default "sms")
      --url string      callback URL for delivery status
```

---

## `plivo message tollfree`

Toll-free verification (US TFN messaging compliance)

```
plivo message tollfree
plivo message tollfree [command]
```

Aliases: `tfv`

Subcommands:

- `get` — Get a toll-free verification profile by uuid
- `list` — List toll-free verification profiles
- `submit` — Submit a new toll-free verification profile

---

## `plivo message tollfree get`

Get a toll-free verification profile by uuid

```
plivo message tollfree get <profile_uuid>
```

---

## `plivo message tollfree list`

List toll-free verification profiles

```
plivo message tollfree list [flags]
```

Flags:

```
      --limit int       results per page (default 20)
      --offset int      pagination offset
      --status string   filter by status: SUBMITTED|IN_REVIEW|APPROVED|REJECTED
```

---

## `plivo message tollfree submit`

Submit a new toll-free verification profile

```
plivo message tollfree submit [flags]
```

Flags:

```
      --business-name string                business name (required)
      --business-website string             business website URL
      --message-volume string               expected volume: LOW|MEDIUM|HIGH
      --numbers string                      comma-separated toll-free numbers to verify
      --opt-in-workflow string              describe how recipients opt in
      --production-message-content string   sample message content
      --use-case string                     use case category (required)
      --use-case-summary string             free-text use case summary
```

---

## `plivo numbers`

Manage account phone numbers

```
plivo numbers
plivo numbers [command]
```

Aliases: `number`

Subcommands:

- `buy` — Rent a phone number (requires --yes; spends money)
- `cnam` — Caller-ID Name (CNAM) lookup for a US/CA number (spends money — requires --yes)
- `compliance` — Regulatory compliance for phone numbers (requirements, applications, linking)
- `get` — Get details of a rented number
- `list` — List numbers rented to your account
- `masking` — Number-Masking sessions (privacy-preserving call/SMS bridge)
- `release` — Release a rented number (requires --yes; stops monthly billing)
- `search` — Search available numbers to rent
- `update` — Update settings on a rented number

---

## `plivo numbers buy`

Rent a phone number (requires --yes; spends money)

```
plivo numbers buy <number> [flags]
```

Flags:

```
      --app-id string   auto-attach to this application after purchase
```

---

## `plivo numbers cnam`

Caller-ID Name (CNAM) lookup for a US/CA number (spends money — requires --yes)

```
plivo numbers cnam <number>
```

---

## `plivo numbers compliance`

Regulatory compliance for phone numbers (requirements, applications, linking)

```
plivo numbers compliance
plivo numbers compliance [command]
```

Subcommands:

- `create` — Create + submit a compliance application (multipart; auto-submits)
- `delete` — Delete a compliance application (requires --yes)
- `get` — Get a compliance application by ID
- `link` — Bulk-link numbers to accepted compliance applications
- `list` — List compliance applications
- `requirements` — List documents/fields required to activate a regulated number
- `update` — Update a rejected compliance application (multipart; auto-resubmits)

---

## `plivo numbers compliance create`

Create + submit a compliance application (multipart; auto-submits)

Create and submit a compliance application for a regulated phone number.
Sent as multipart/form-data: the --data JSON is the "data" part and each --file
is an uploaded document.

--data (inline or @file.json) is the application payload:

  {
    "country_iso": "US",
    "number_type": "local",                 // local | mobile | tollfree
    "alias": "acme-us-local",
    "end_user": {
      "type": "business",                   // individual | business
      "name": "Acme Inc",
      "email": "ops@acme.com"
    },
    "documents": [
      { "document_type_id": "<id>", "data_fields": { "registration_number": "..." } }
    ],
    "callback_url": "https://example.com/compliance"   // optional
  }

--file attaches one upload per document; the field name maps positionally to the
documents[] array, so documents[0].file is the file for documents[0]. The path
may be bare or prefixed with @ (PDF/JPEG/PNG, max 5 MB).

Discover the document_type_id values and required data_fields first with
`plivo numbers compliance requirements ...`.

```
plivo numbers compliance create [flags]
```

Examples:

```
  plivo numbers compliance create \
    --data @app.json \
    --file documents[0].file=@passport.pdf \
    --file documents[1].file=@address-proof.pdf
```

Flags:

```
      --data string        application JSON; inline or @file.json (required)
      --file stringArray   document upload as field=path, e.g. documents[0].file=@id.pdf (repeatable)
```

---

## `plivo numbers compliance delete`

Delete a compliance application (requires --yes)

```
plivo numbers compliance delete <compliance_id>
```

---

## `plivo numbers compliance get`

Get a compliance application by ID

```
plivo numbers compliance get <compliance_id> [flags]
```

Examples:

```
  plivo numbers compliance get <compliance_id> --expand end_user,documents,linked_numbers
```

Flags:

```
      --expand string   comma-separated: end_user,documents,linked_numbers
```

---

## `plivo numbers compliance link`

Bulk-link numbers to accepted compliance applications

Associate rented numbers with accepted compliance applications. Repeat --link
once per number, or pass the full JSON body via --data (inline or @file.json):
{"numbers":[{"number":"+14155551234","compliance_application_id":"<id>"}]}.

```
plivo numbers compliance link [flags]
```

Examples:

```
  plivo numbers compliance link --link +14155551234=<compliance_id> --link +14155556789=<compliance_id>
```

Flags:

```
      --data string        full link JSON body; inline or @file.json (alternative to --link)
      --link stringArray   number=compliance_application_id (repeatable)
```

---

## `plivo numbers compliance list`

List compliance applications

```
plivo numbers compliance list [flags]
```

Flags:

```
      --alias string         filter by alias
      --country string       filter by ISO country code
      --limit int            results per page (default 20)
      --number-type string   filter by number type
      --offset int           pagination offset
      --status string        filter by status
      --user-type string     filter by user type
```

---

## `plivo numbers compliance requirements`

List documents/fields required to activate a regulated number

Returns the document types and required data fields for a given country / number type
/ user type — use the returned document_type_id values when building a `create` payload.

```
plivo numbers compliance requirements [flags]
```

Examples:

```
  plivo numbers compliance requirements --country US --number-type local --user-type business
```

Flags:

```
      --country string       ISO country code, e.g. US (required)
      --number-type string   local|mobile|tollfree (required)
      --user-type string     individual|business (required)
```

---

## `plivo numbers compliance update`

Update a rejected compliance application (multipart; auto-resubmits)

Update a REJECTED compliance application and resubmit it. Same --data / --file
shape as create. Documents are FULLY REPLACED — re-attach every required file,
not only the changed ones. Only applications in 'rejected' status can be updated.

```
plivo numbers compliance update <compliance_id> [flags]
```

Examples:

```
  plivo numbers compliance update <compliance_id> --data @fixed.json --file documents[0].file=@passport.pdf
```

Flags:

```
      --data string        updated application JSON; inline or @file.json (required)
      --file stringArray   document upload as field=path (repeatable; replaces all documents)
```

---

## `plivo numbers get`

Get details of a rented number

```
plivo numbers get <number>
```

---

## `plivo numbers list`

List numbers rented to your account

```
plivo numbers list [flags]
```

Flags:

```
      --alias string         filter by alias
      --limit int            results per page (max 20) (default 20)
      --offset int           pagination offset
      --services string      filter by services: voice|sms|mms|voice,sms ...
      --starts-with string   prefix filter on E.164
      --subaccount string    filter by subaccount auth_id
      --type string          filter by type: local|tollfree|mobile|fixed
```

---

## `plivo numbers masking`

Number-Masking sessions (privacy-preserving call/SMS bridge)

```
plivo numbers masking
plivo numbers masking [command]
```

Aliases: `mask`

Subcommands:

- `sessions` — Manage number-masking sessions

---

## `plivo numbers masking sessions`

Manage number-masking sessions

```
plivo numbers masking sessions
plivo numbers masking sessions [command]
```

Aliases: `session`

Subcommands:

- `create` — Create a masking session (spends money — requires --yes)
- `delete` — End a masking session (requires --yes)
- `get` — Get a masking session by uuid
- `list` — List masking sessions

---

## `plivo numbers masking sessions create`

Create a masking session (spends money — requires --yes)

```
plivo numbers masking sessions create [flags]
```

Flags:

```
      --call-time-limit int     max per-call duration in seconds
      --first-party string      first party E.164 (required)
      --mode string             voice|sms|both (default "both")
      --record                  record calls in this session
      --second-party string     second party E.164 (required)
      --session-expiry int      session lifetime in seconds
      --virtual-number string   virtual number to use (else allocates from pool)
```

---

## `plivo numbers masking sessions delete`

End a masking session (requires --yes)

```
plivo numbers masking sessions delete <session_uuid>
```

---

## `plivo numbers masking sessions get`

Get a masking session by uuid

```
plivo numbers masking sessions get <session_uuid>
```

---

## `plivo numbers masking sessions list`

List masking sessions

```
plivo numbers masking sessions list [flags]
```

Flags:

```
      --limit int    results per page (default 20)
      --offset int   pagination offset
```

---

## `plivo numbers release`

Release a rented number (requires --yes; stops monthly billing)

```
plivo numbers release <number>
```

---

## `plivo numbers search`

Search available numbers to rent

```
plivo numbers search [flags]
```

Flags:

```
      --country string   ISO country code, e.g. US (required)
      --limit int        results per page (default 20)
      --offset int       pagination offset
      --pattern string   digit pattern
      --region string    region filter
      --type string      local|tollfree|mobile|fixed
```

---

## `plivo numbers update`

Update settings on a rented number

```
plivo numbers update <number> [flags]
```

Flags:

```
      --alias string        set alias
      --app-id string       associate an application
      --subaccount string   move under subaccount
```

---

## `plivo support`

List your past support escalations (filed via `plivo ask`)

```
plivo support [flags]
```

Examples:

```
  plivo support
  plivo support -o json
```

Flags:

```
      --buddy-url string   override the assistant's hodor URL (also via PLIVO_BUDDY_URL env, or [buddy].hodor_url in config)
```

---

## `plivo upgrade`

Upgrade plivo to the latest release

Upgrade plivo to the latest GitHub release.

Fetches the latest release tag, compares it to the running version, then
downloads the matching binary for your OS/arch and atomically replaces the
current binary in place.

If you installed via Homebrew this command will refuse — use `brew upgrade plivo` instead.

```
plivo upgrade [flags]
```

Examples:

```
  plivo upgrade                 # install latest
  plivo upgrade --check         # check only, don't install
  plivo upgrade --version v0.2.0
  plivo upgrade --force         # reinstall even if already on latest
```

Flags:

```
      --check            report whether a newer release exists, but don't install
      --force            reinstall even if the running binary is already on the target release
      --version string   install a specific release tag instead of latest (e.g. v0.2.0)
```

---

## `plivo verify`

Plivo Verify — OTP / phone-number verification sessions

```
plivo verify
plivo verify [command]
```

Subcommands:

- `sessions` — Manage Verify sessions

---

## `plivo verify sessions`

Manage Verify sessions

```
plivo verify sessions
plivo verify sessions [command]
```

Aliases: `session`

Subcommands:

- `create` — Create a Verify session (spends money — requires --yes)
- `get` — Get a Verify session by uuid
- `list` — List Verify sessions
- `validate` — Submit the OTP to validate a session

---

## `plivo verify sessions create`

Create a Verify session (spends money — requires --yes)

```
plivo verify sessions create [flags]
```

Flags:

```
      --alpha-sender string   alphanumeric sender id
      --app-uuid string       Verify application uuid (required)
      --channel string        delivery channel: sms|voice|whatsapp (default "sms")
      --locale string         BCP-47 locale, e.g. en-US
      --method string         HTTP method for callback URL
      --recipient string      E.164 destination number (required)
      --url string            callback URL for session status events
```

---

## `plivo verify sessions get`

Get a Verify session by uuid

```
plivo verify sessions get <session_uuid>
```

---

## `plivo verify sessions list`

List Verify sessions

```
plivo verify sessions list [flags]
```

Flags:

```
      --limit int       results per page (default 20)
      --offset int      pagination offset
      --status string   filter by status: pending|verified|expired
```

---

## `plivo verify sessions validate`

Submit the OTP to validate a session

```
plivo verify sessions validate <session_uuid> [flags]
```

Flags:

```
      --otp string   OTP code received by the recipient (required)
```

---

## `plivo voice`

Voice — calls, conferences, multiparty, recordings, endpoints

```
plivo voice
plivo voice [command]
```

Subcommands:

- `calls` — Make and inspect voice calls
- `conferences` — Inspect and control live audio conferences
- `endpoints` — Manage SIP endpoints (registered SIP devices/usernames)
- `multiparty` — Multi-Party Calls (MPC) — group voice rooms with dynamic participants
- `recordings` — List, fetch, and delete call/conference recordings

---

## `plivo voice calls`

Make and inspect voice calls

```
plivo voice calls
plivo voice calls [command]
```

Aliases: `call`

Subcommands:

- `dtmf` — Send DTMF tones into a live call
- `get` — Get a call by UUID
- `hangup` — Hang up a live call (requires --yes)
- `list` — List calls
- `make` — Make an outbound call (defaults to dry-run; pass --yes to actually dial)
- `play` — Play audio file(s) into a live call
- `record` — Start recording a live call
- `speak` — Speak text into a live call via TTS
- `stop-play` — Stop any audio currently playing in a live call
- `stop-record` — Stop the active recording on a live call
- `stop-speak` — Stop any TTS currently playing in a live call
- `streams` — Live audio streams on a call (WebSocket bridge for transcription / agents)
- `transfer` — Transfer one or both legs of a live call to new PlivoXML URLs

---

## `plivo voice calls dtmf`

Send DTMF tones into a live call

```
plivo voice calls dtmf <call_uuid> [flags]
```

Flags:

```
      --digits string   DTMF digits to send, e.g. 1234# (required)
      --leg string      aleg|bleg (default "aleg")
```

---

## `plivo voice calls get`

Get a call by UUID

```
plivo voice calls get <call_uuid>
```

---

## `plivo voice calls hangup`

Hang up a live call (requires --yes)

```
plivo voice calls hangup <call_uuid>
```

---

## `plivo voice calls list`

List calls

```
plivo voice calls list [flags]
```

Flags:

```
      --direction string   inbound|outbound
      --from string        filter by from_number
      --limit int          results per page (default 20)
      --offset int         pagination offset
      --to string          filter by to_number
```

---

## `plivo voice calls make`

Make an outbound call (defaults to dry-run; pass --yes to actually dial)

```
plivo voice calls make [flags]
```

Flags:

```
      --answer-method string       GET|POST (default "GET")
      --answer-url string          URL returning PlivoXML to play on answer (default: Plivo's hello demo) (default "https://s3.amazonaws.com/static.plivo.com/answer.xml")
      --from string                source number (E.164) — must be on your account (required)
      --hangup-url string          URL hit when call ends
      --machine-detection string   none|true|hangup
      --ring-url string            URL hit when call starts ringing
      --to string                  destination number (E.164) (required)
```

---

## `plivo voice calls play`

Play audio file(s) into a live call

```
plivo voice calls play <call_uuid> [flags]
```

Flags:

```
      --legs string   aleg|bleg|both (default "aleg")
      --length int    stop after N seconds (0 = full file)
      --loop          loop the playback
      --mix           mix with the call audio (else replace) (default true)
      --urls string   comma-separated list of audio file URLs to play (required)
```

---

## `plivo voice calls record`

Start recording a live call

```
plivo voice calls record <call_uuid> [flags]
```

Flags:

```
      --both-legs                record both legs (default: just A-leg)
      --callback-method string   GET|POST (default "POST")
      --callback-url string      URL hit when recording finishes
      --file-format string       mp3|wav (default "mp3")
      --time-limit int           max recording length in seconds (default 60)
      --transcribe               request transcription
```

---

## `plivo voice calls speak`

Speak text into a live call via TTS

```
plivo voice calls speak <call_uuid> [flags]
```

Flags:

```
      --language string   language code, e.g. en-US, en-GB, hi-IN (default "en-US")
      --legs string       aleg|bleg|both (default "aleg")
      --mix               mix with the call audio (else replace) (default true)
      --text string       text to speak via TTS (required)
      --voice string      voice: MAN|WOMAN (default "WOMAN")
```

---

## `plivo voice calls stop-play`

Stop any audio currently playing in a live call

```
plivo voice calls stop-play <call_uuid>
```

---

## `plivo voice calls stop-record`

Stop the active recording on a live call

```
plivo voice calls stop-record <call_uuid>
```

---

## `plivo voice calls stop-speak`

Stop any TTS currently playing in a live call

```
plivo voice calls stop-speak <call_uuid>
```

---

## `plivo voice calls streams`

Live audio streams on a call (WebSocket bridge for transcription / agents)

```
plivo voice calls streams
plivo voice calls streams [command]
```

Aliases: `stream`

Subcommands:

- `get` — Get details for one stream on a call
- `list` — List active streams on a call
- `start` — Start a new audio stream on a call
- `stop` — Stop one stream (with id) or all streams (without id) on a call

---

## `plivo voice calls streams get`

Get details for one stream on a call

```
plivo voice calls streams get <call_uuid> <stream_id>
```

---

## `plivo voice calls streams list`

List active streams on a call

```
plivo voice calls streams list <call_uuid>
```

---

## `plivo voice calls streams start`

Start a new audio stream on a call

```
plivo voice calls streams start <call_uuid> [flags]
```

Flags:

```
      --audio-track string              inbound|outbound|both (default "inbound")
      --bidirectional                   let the WebSocket send audio back into the call
      --callback-url string             alias for --stream-status-callback
      --content-type string             audio codec content-type (default "audio/x-l16;rate=16000")
      --extra-headers string            comma-separated extra WebSocket headers (k1=v1,k2=v2)
      --service-type string             Plivo service type override
      --stream-status-callback string   URL hit when stream starts/ends
      --url string                      WebSocket URL to receive audio (wss://...) (required)
```

---

## `plivo voice calls streams stop`

Stop one stream (with id) or all streams (without id) on a call

```
plivo voice calls streams stop <call_uuid> [stream_id]
```

---

## `plivo voice calls transfer`

Transfer one or both legs of a live call to new PlivoXML URLs

```
plivo voice calls transfer <call_uuid> [flags]
```

Flags:

```
      --aleg-method string   GET|POST (default "POST")
      --aleg-url string      new URL for A-leg (caller side)
      --bleg-method string   GET|POST (default "POST")
      --bleg-url string      new URL for B-leg (callee side)
      --legs string          which leg(s) to act on: aleg|bleg|both (default "aleg")
```

---

## `plivo voice conferences`

Inspect and control live audio conferences

```
plivo voice conferences
plivo voice conferences [command]
```

Aliases: `conf`, `conference`

Subcommands:

- `get` — Get conference details (members, run time)
- `hangup` — End the entire conference (requires --yes)
- `list` — List active conference names
- `member` — Per-member actions inside a conference
- `record` — Start recording a conference
- `stop-record` — Stop the active recording on a conference

---

## `plivo voice conferences get`

Get conference details (members, run time)

```
plivo voice conferences get <conference_name>
```

---

## `plivo voice conferences hangup`

End the entire conference (requires --yes)

```
plivo voice conferences hangup <conference_name>
```

---

## `plivo voice conferences list`

List active conference names

```
plivo voice conferences list
```

---

## `plivo voice conferences member`

Per-member actions inside a conference

```
plivo voice conferences member
plivo voice conferences member [command]
```

Subcommands:

- `deaf` — Deafen a member (they can't hear)
- `kick` — Kick a member from the conference (requires --yes)
- `mute` — Mute a member
- `play` — Play audio file(s) into a member's channel
- `speak` — Speak TTS text to a member
- `stop-play` — Stop audio playback to a member
- `stop-speak` — Stop TTS playback to a member
- `undeaf` — Undeafen a member
- `unmute` — Unmute a member

---

## `plivo voice conferences member deaf`

Deafen a member (they can't hear)

```
plivo voice conferences member deaf <conference_name> <member_id>
```

---

## `plivo voice conferences member kick`

Kick a member from the conference (requires --yes)

```
plivo voice conferences member kick <conference_name> <member_id>
```

---

## `plivo voice conferences member mute`

Mute a member

```
plivo voice conferences member mute <conference_name> <member_id>
```

---

## `plivo voice conferences member play`

Play audio file(s) into a member's channel

```
plivo voice conferences member play <conference_name> <member_id> [flags]
```

Flags:

```
      --length int    stop after N seconds
      --loop          loop playback
      --mix           mix with conference audio (default true)
      --urls string   comma-separated audio file URLs (required)
```

---

## `plivo voice conferences member speak`

Speak TTS text to a member

```
plivo voice conferences member speak <conference_name> <member_id> [flags]
```

Flags:

```
      --language string   BCP-47 language code (default "en-US")
      --text string       TTS text (required)
      --voice string      MAN|WOMAN (default "WOMAN")
```

---

## `plivo voice conferences member stop-play`

Stop audio playback to a member

```
plivo voice conferences member stop-play <conference_name> <member_id>
```

---

## `plivo voice conferences member stop-speak`

Stop TTS playback to a member

```
plivo voice conferences member stop-speak <conference_name> <member_id>
```

---

## `plivo voice conferences member undeaf`

Undeafen a member

```
plivo voice conferences member undeaf <conference_name> <member_id>
```

---

## `plivo voice conferences member unmute`

Unmute a member

```
plivo voice conferences member unmute <conference_name> <member_id>
```

---

## `plivo voice conferences record`

Start recording a conference

```
plivo voice conferences record <conference_name> [flags]
```

Flags:

```
      --callback-url string   URL hit when recording finishes
      --file-format string    mp3|wav (default "mp3")
      --time-limit int        max recording length in seconds (default 60)
      --transcribe            request transcription
```

---

## `plivo voice conferences stop-record`

Stop the active recording on a conference

```
plivo voice conferences stop-record <conference_name>
```

---

## `plivo voice endpoints`

Manage SIP endpoints (registered SIP devices/usernames)

```
plivo voice endpoints
plivo voice endpoints [command]
```

Aliases: `ep`, `endpoint`

Subcommands:

- `create` — Create a SIP endpoint
- `delete` — Delete a SIP endpoint (requires --yes)
- `get` — Get an endpoint by ID
- `list` — List SIP endpoints
- `update` — Update a SIP endpoint

---

## `plivo voice endpoints create`

Create a SIP endpoint

```
plivo voice endpoints create [flags]
```

Flags:

```
      --alias string      human-friendly label
      --app-id string     application to attach
      --password string   SIP password (required)
      --username string   SIP username (required)
```

---

## `plivo voice endpoints delete`

Delete a SIP endpoint (requires --yes)

```
plivo voice endpoints delete <endpoint_id>
```

---

## `plivo voice endpoints get`

Get an endpoint by ID

```
plivo voice endpoints get <endpoint_id>
```

---

## `plivo voice endpoints list`

List SIP endpoints

```
plivo voice endpoints list [flags]
```

Flags:

```
      --limit int    results per page (default 20)
      --offset int   pagination offset
```

---

## `plivo voice endpoints update`

Update a SIP endpoint

```
plivo voice endpoints update <endpoint_id> [flags]
```

Flags:

```
      --alias string      new alias
      --app-id string     new application id
      --password string   new password
```

---

## `plivo voice multiparty`

Multi-Party Calls (MPC) — group voice rooms with dynamic participants

```
plivo voice multiparty
plivo voice multiparty [command]
```

Aliases: `mpc`

Subcommands:

- `create` — Create a multi-party call (spends money — requires --yes)
- `end` — End the MPC and hang up all participants (requires --yes)
- `get` — Get an MPC by uuid or friendly_name
- `list` — List multi-party calls
- `participant` — Per-participant actions inside an MPC

---

## `plivo voice multiparty create`

Create a multi-party call (spends money — requires --yes)

```
plivo voice multiparty create [flags]
```

Flags:

```
      --max-participants int   cap on simultaneous participants
      --name string            friendly_name for the MPC (required)
      --record                 auto-record the MPC
```

---

## `plivo voice multiparty end`

End the MPC and hang up all participants (requires --yes)

```
plivo voice multiparty end <mpc_uuid_or_name>
```

---

## `plivo voice multiparty get`

Get an MPC by uuid or friendly_name

```
plivo voice multiparty get <mpc_uuid_or_name>
```

---

## `plivo voice multiparty list`

List multi-party calls

```
plivo voice multiparty list [flags]
```

Flags:

```
      --limit int       results per page (default 20)
      --offset int      pagination offset
      --status string   filter by status: active|initialized|ended
```

---

## `plivo voice multiparty participant`

Per-participant actions inside an MPC

```
plivo voice multiparty participant
plivo voice multiparty participant [command]
```

Aliases: `part`

Subcommands:

- `add` — Add a participant by dialing out (spends money — requires --yes)
- `hold` — Put a participant on hold
- `kick` — Remove a participant (requires --yes)
- `list` — List participants in an MPC
- `mute` — Mute a participant
- `unhold` — Take a participant off hold
- `unmute` — Unmute a participant

---

## `plivo voice multiparty participant add`

Add a participant by dialing out (spends money — requires --yes)

```
plivo voice multiparty participant add <mpc_uuid_or_name> [flags]
```

Flags:

```
      --from string   source number for the dial-out (required)
      --role string   participant role: agent|supervisor|customer (default "agent")
      --to string     destination number to add (required)
```

---

## `plivo voice multiparty participant hold`

Put a participant on hold

```
plivo voice multiparty participant hold <mpc_uuid_or_name> <participant_id>
```

---

## `plivo voice multiparty participant kick`

Remove a participant (requires --yes)

```
plivo voice multiparty participant kick <mpc_uuid_or_name> <participant_id>
```

---

## `plivo voice multiparty participant list`

List participants in an MPC

```
plivo voice multiparty participant list <mpc_uuid_or_name> [flags]
```

Flags:

```
      --limit int    results per page (default 20)
      --offset int   pagination offset
```

---

## `plivo voice multiparty participant mute`

Mute a participant

```
plivo voice multiparty participant mute <mpc_uuid_or_name> <participant_id>
```

---

## `plivo voice multiparty participant unhold`

Take a participant off hold

```
plivo voice multiparty participant unhold <mpc_uuid_or_name> <participant_id>
```

---

## `plivo voice multiparty participant unmute`

Unmute a participant

```
plivo voice multiparty participant unmute <mpc_uuid_or_name> <participant_id>
```

---

## `plivo voice recordings`

List, fetch, and delete call/conference recordings

```
plivo voice recordings
plivo voice recordings [command]
```

Aliases: `rec`, `recording`

Subcommands:

- `delete` — Delete a recording (requires --yes)
- `get` — Get a recording by ID
- `list` — List recordings

---

## `plivo voice recordings delete`

Delete a recording (requires --yes)

```
plivo voice recordings delete <recording_id>
```

---

## `plivo voice recordings get`

Get a recording by ID

```
plivo voice recordings get <recording_id>
```

---

## `plivo voice recordings list`

List recordings

```
plivo voice recordings list [flags]
```

Flags:

```
      --call-uuid string         filter by call uuid
      --conference-name string   filter by conference name
      --from-time string         filter recordings after this ISO time
      --limit int                results per page (default 20)
      --offset int               pagination offset
      --to-time string           filter recordings before this ISO time
```

